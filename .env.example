# AI 播客生成系统环境变量
#
# 【重要】本仓库没有任何代码加载 .env 文件——它是一份变量清单，不是配置
# 入口。三种用法：
#   1) 直接导出：      export MODELS_ROOT=/models
#   2) source 一个副本：set -a; source .env; set +a
#   3) 交给 Docker：   docker run --env-file .env ...
# 只把文件放在仓库根目录、不做上面任何一步，这些变量**不会生效**。

# 模型挂载点（scripts/generate.py 会读取它；优先级：--models-root > MODELS_ROOT > 配置文件）
MODELS_ROOT=/models

# HuggingFace 镜像（拉取模型用；MOSS-TTS-GGUF 为 gated 仓库时需 Token）
HF_ENDPOINT=https://hf-mirror.com
# HF_TOKEN=hf_xxx

# 编译 llama.cpp 时的 CUDA 架构（A100 是 80，不要用消费级卡的 86/89）
CUDA_ARCH=80

# LLM 服务的地址由 configs/default.yaml 的 llm.base_url 决定；沿用这里的环境
# 变量不会改变它（两个入口都不读 LLM_BASE_URL）。要换地址请改配置文件。
